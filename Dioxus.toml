[application]
default_width = 1200
default_height = 800
resizable = false

[desktop]
default_width = 1200
default_height = 800
resizable = false

# HTML title tag content
title = "pattern-clock"

# Skip WASM builds - desktop-only app
# When using --platform desktop, Dioxus will skip WASM compilation
[web.app]
skip_assets = true
